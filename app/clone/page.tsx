import SaphexHero from "@/components/clone/SaphexHero";

export const metadata = {
  title: "SAPHEX Labs — Magic as a Service",
  description: "SAPHEX Labs hero section clone",
};

export default function ClonePage() {
  return <SaphexHero />;
}
